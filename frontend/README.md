# Shoes Store - Frontend

Интернет-магазин обуви с современным React-интерфейсом.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Запуск приложения
```bash
npm start
```

Приложение будет доступно по адресу: http://localhost:3000

## 📋 Функциональность

### ✅ Реализовано:
- **Авторизация и регистрация** — красивые формы входа и регистрации
- **Каталог товаров** — просмотр всех товаров с фильтрацией по брендам и категориям
- **Корзина** — добавление товаров, изменение количества, удаление
- **Избранное** — сохранение любимых товаров
- **Навигационное меню** — переключение между страницами
- **Защита роутов** — только авторизованные пользователи могут добавлять товары

### 🎨 Дизайн:
- Современный градиентный дизайн
- Адаптивная верстка
- Плавные анимации и переходы
- Удобный UX

## 🔗 Подключение к Backend

Backend должен быть запущен на `http://localhost:8080`

Для изменения адреса API отредактируйте файл `src/api.js`:
```javascript
const API_BASE_URL = 'http://localhost:8080';
```

## 📁 Структура проекта

```
src/
├── api.js                    # API клиент с axios
├── AuthContext.js            # Контекст авторизации
├── App.js                    # Главный компонент с роутингом
├── App.css                   # Стили приложения
├── components/
│   ├── Navigation.js         # Навигационное меню
│   └── Navigation.css
└── pages/
    ├── Login.js              # Страница входа
    ├── Register.js           # Страница регистрации
    ├── Catalog.js            # Каталог товаров
    ├── Basket.js             # Корзина
    ├── Favorites.js          # Избранное
    ├── Auth.css              # Стили форм
    ├── Catalog.css           # Стили каталога
    ├── Basket.css            # Стили корзины
    └── Favorites.css         # Стили избранного
```

## 🔑 Роли пользователей

- **User (roleID: 3)** — базовые пользователи
- **Admin (roleID: 1)** — администраторы (через backend)
- **Manager (roleID: 2)** — менеджеры (через backend)

## 📝 API Endpoints

Приложение использует следующие API endpoints:

- `POST /register` — регистрация
- `POST /login` — авторизация
- `GET /products` — получение товаров
- `GET /brands` — получение брендов
- `GET /categories` — получение категорий
- `GET /basket/{user_id}` — корзина
- `GET /favorites/{user_id}` — избранное

## 🚧 Что можно добавить:

1. Страницу товара с детальной информацией
2. Поиск по каталогу
3. Сортировку товаров (по цене, названию)
4. Оформление заказа
5. Профиль пользователя
6. История заказов
